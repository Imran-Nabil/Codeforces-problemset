#include <stdio.h>
int main() {
    long long n,m,a;
    scanf("%lld %lld %lld",&n,&m,&a);
 
    long long row = (n+a-1)/a;
    long long col = (m+a-1)/a;
    long long blocks = row*col;
 
    printf("%lld\n",blocks);
    return 0;
}
